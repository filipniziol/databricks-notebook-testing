-- Databricks notebook source
-- MAGIC %md
-- MAGIC # Create Schema: poker.ml
-- MAGIC Schema for ML models, features, and predictions.

-- COMMAND ----------

CREATE SCHEMA IF NOT EXISTS poker.ml
COMMENT 'ML schema for models, feature tables, and predictions'
